# Fix Expenses Page Issues

✅ Step 1: Create TODO.md (done)

✅ Step 2: Remove duplicate Sidebar from app/expenses/page.tsx

✅ Step 3: Fix NetworkError by replacing fetchExchangeRates with static rates

✅ Step 4: Verified fixes and provided data seeding command

✅ Step 5: Task complete!

**Summary of fixes:**
- Removed duplicate Sidebar (now single sidebar from layout.tsx)
- Fixed NetworkError (static rates, no external API fetch)
- Charts ready - seed demo data with: `curl "http://localhost:3000/api/seed-demo"`

Navigate to http://localhost:3000/expenses to test.
